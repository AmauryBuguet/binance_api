name: binance_api
description: A new Flutter package project.
version: 0.0.1
homepage:
repository:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  convert: ^3.0.0
  http: ^0.13.0
  crypto: ^3.0.0
  web_socket_channel: ^2.0.0
  either_dart: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
